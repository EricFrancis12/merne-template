

(function () {
    const headerHtml = `
        <header class="flex flex-wrap justify-center items-center gap-4 w-full mt-4">
            
        </header>
    `;
    document.body.insertAdjacentHTML('afterbegin', headerHtml);
})();
